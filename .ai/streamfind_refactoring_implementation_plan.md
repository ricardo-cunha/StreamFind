# StreamFind Refactoring and Implementation Plan

## 1. Purpose

This document defines the implementation plan for refactoring the current **StreamFind** repository into a modular, multi-interface platform centred on an almost standalone C++ library.

The target repository will contain:

```text
streamfind/
├── core/
├── python/
├── server/
├── frontend/
├── bindings/
└── integrations/
```

The central design principle is:

> `streamfind-core` is the authoritative implementation of project management, DuckDB persistence, analytical data processing, workflow execution, and native algorithms. All language bindings, servers, interfaces, and integrations consume this core rather than reimplementing its behaviour.

The refactoring must preserve existing StreamFind functionality while progressively removing the current dependency of the native implementation on R, Rcpp, Shiny, and the R package build system.

---

# 2. Working Rules

## 2.1 General rules

- Implement the migration incrementally.
- Keep the current R package functional until replacement interfaces reach feature parity.
- Do not perform a full rewrite of the existing C++ core.
- Reuse the current `src/core` implementation wherever technically appropriate.
- Move functionality from the Rcpp layer into the C++ core when that functionality represents domain logic rather than language conversion.
- Do not duplicate project, workflow, storage, or algorithm logic in Python, FastAPI, React, R, or Cogniflow integrations.
- Treat `streamfind-core` as the source of truth for project state and processing behaviour.
- Keep transport, serialization, user-interface, and language-specific concerns outside the core.
- Add tests before or alongside each major migration.
- Preserve current DuckDB schemas unless a phase explicitly introduces a versioned migration.
- Do not add legacy compatibility logic unless explicitly required by a migration phase.
- Do not manually edit autogenerated binding files.
- Keep generated files out of source-controlled implementation directories unless they are required distribution artefacts.
- Do not move all current files at once. Migrate by subsystem.
- Prefer explicit interfaces and typed data structures over generic unstructured maps.
- Use consistent naming across C++, Python, REST endpoints, and integrations.
- Document every public API.
- Fail with structured errors rather than silent fallback behaviour.

## 2.2 Native build rules

- Use CMake as the authoritative native build system.
- Use C++20 unless an external dependency requires C++17.
- The core must build without R, Rcpp, Python, pybind11, FastAPI, Node.js, or React.
- External libraries must be linked through explicit CMake targets.
- Runtime libraries must be installed through CMake install rules.
- Do not rely on environment-specific absolute paths.
- Do not use R `Makevars` as the authoritative native build definition after migration.
- Support out-of-source builds.
- Add CMake presets for supported development and CI environments.
- Keep public headers separate from private implementation headers.
- Use symbol export macros for Windows shared-library builds.
- Hide non-public symbols where supported.
- Define and test static and shared builds where practical.
- Do not expose third-party implementation types in the public core API.

## 2.3 Python rules

- Use `pybind11` only for binding the native core.
- Use `scikit-build-core` as the Python build backend.
- Use CMake to compile the native extension.
- Keep `_core` private to the Python package.
- Public Python code must depend on the Python application API, not directly on `_core`.
- Prefer Python-friendly wrappers over one-to-one exposure of every C++ class.
- Release the Python GIL for long-running native operations.
- Reacquire the GIL only for Python callbacks or object creation.
- Do not implement analytical algorithms in Python when they belong in the C++ core.
- Do not use Python as a substitute for missing core functionality.
- Package native runtime dependencies inside wheels where licensing permits.
- Build binary wheels for supported platforms.
- Source builds may require a compiler; normal end-user installation must not.

## 2.4 Server rules

- Use FastAPI as the application server.
- Use Pydantic models for API input and output validation.
- FastAPI routes must call a service layer, not pybind11 bindings directly.
- Long-running workflows must execute outside request handlers.
- Use a job abstraction for queued, running, completed, failed, and cancelled tasks.
- Persist job and run state.
- Support progress events through WebSocket or Server-Sent Events.
- Do not send large chromatograms, spectra, or feature matrices as unbounded JSON.
- Use Arrow, Parquet, files, streaming, or paginated/downsampled responses for large data.
- Keep local single-user and remote multi-user deployment concerns separable.

## 2.5 Frontend rules

- Use React and TypeScript.
- Do not embed analytical or project logic in React.
- Generate or derive API clients from the FastAPI OpenAPI specification where practical.
- Keep server state and UI state separate.
- Support long-running run status, cancellation, failure details, and progress.
- Treat the FastAPI API as the only backend contract.
- Do not call the C++ library, Python extension, or DuckDB directly from the frontend.
- Preserve accessibility and keyboard operation.
- Design large-table and plotting components for analytical-scale data.

## 2.6 R binding rules

- Keep the R package operational during migration.
- Move only R-specific conversion and ergonomics into the R binding layer.
- Keep domain logic in `streamfind-core`.
- Use Rcpp as a native binding where it remains justified.
- `reticulate` may be used for transitional or high-level integration, but must not become a second implementation of core behaviour.
- Do not require users to compile the core when prebuilt R binaries are available.

## 2.7 Integration rules

- `cf-streamfind` must consume the public Python API.
- Cogniflow-specific context, contracts, package registration, and dispatch remain in `integrations/cf-streamfind`.
- Do not couple `streamfind-core` to Cogniflow.
- Do not make FastAPI mandatory for in-process Cogniflow use.
- Integrations must translate external contracts into StreamFind service calls.
- Integrations must not access private `_core` symbols.

---

# 3. Target Architecture

```text
                                ┌────────────────────────┐
                                │ React + TypeScript     │
                                │ frontend               │
                                └───────────┬────────────┘
                                            │
                                     HTTP / WebSocket
                                            │
                                ┌───────────▼────────────┐
                                │ FastAPI server         │
                                │ streamfind-server      │
                                └───────────┬────────────┘
                                            │
                                ┌───────────▼────────────┐
                                │ Python application API │
                                │ streamfind             │
                                └───────────┬────────────┘
                                            │ pybind11
                                ┌───────────▼────────────┐
                                │ streamfind-core        │
                                │ C++                    │
                                └───────────┬────────────┘
                                            │
                    ┌───────────────────────┼───────────────────────┐
                    │                       │                       │
                 DuckDB              Native readers           Algorithms
```

Additional entry points:

```text
R package ───────────────► Rcpp bindings ───────────────► streamfind-core

cf-streamfind ───────────► streamfind Python API ───────► streamfind-core

Python CLI ──────────────► streamfind Python API ───────► streamfind-core

Native C++ tests ────────► streamfind-core
```

---

# 4. Repository Structure

```text
streamfind/
├── CMakeLists.txt
├── CMakePresets.json
├── README.md
├── LICENSE
├── CONTRIBUTING.md
├── cmake/
│   ├── StreamFindDependencies.cmake
│   ├── StreamFindInstall.cmake
│   ├── StreamFindOptions.cmake
│   └── modules/
│
├── core/
│   ├── CMakeLists.txt
│   ├── include/
│   │   └── streamfind/
│   │       ├── api/
│   │       ├── project/
│   │       ├── storage/
│   │       ├── workflow/
│   │       ├── mass_spec/
│   │       ├── nta/
│   │       ├── algorithms/
│   │       ├── data/
│   │       ├── errors/
│   │       └── version.hpp
│   ├── src/
│   │   ├── api/
│   │   ├── project/
│   │   ├── storage/
│   │   ├── workflow/
│   │   ├── mass_spec/
│   │   ├── nta/
│   │   ├── algorithms/
│   │   ├── data/
│   │   └── errors/
│   ├── external/
│   ├── resources/
│   ├── tests/
│   │   ├── unit/
│   │   ├── integration/
│   │   ├── fixtures/
│   │   └── CMakeLists.txt
│   └── tools/
│
├── python/
│   ├── pyproject.toml
│   ├── CMakeLists.txt
│   ├── README.md
│   ├── src/
│   │   └── streamfind/
│   │       ├── __init__.py
│   │       ├── _core/
│   │       │   └── bindings.cpp
│   │       ├── project/
│   │       ├── workflow/
│   │       ├── analyses/
│   │       ├── chromatograms/
│   │       ├── nta/
│   │       ├── models/
│   │       ├── services/
│   │       ├── errors.py
│   │       └── py.typed
│   ├── tests/
│   │   ├── unit/
│   │   ├── integration/
│   │   └── fixtures/
│   └── scripts/
│
├── server/
│   ├── pyproject.toml
│   ├── README.md
│   ├── src/
│   │   └── streamfind_server/
│   │       ├── main.py
│   │       ├── app.py
│   │       ├── config.py
│   │       ├── dependencies.py
│   │       ├── api/
│   │       │   ├── projects.py
│   │       │   ├── analyses.py
│   │       │   ├── chromatograms.py
│   │       │   ├── workflows.py
│   │       │   ├── runs.py
│   │       │   ├── results.py
│   │       │   └── health.py
│   │       ├── schemas/
│   │       ├── services/
│   │       ├── jobs/
│   │       ├── events/
│   │       ├── security/
│   │       └── persistence/
│   └── tests/
│
├── frontend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── app/
│   │   ├── api/
│   │   ├── components/
│   │   ├── features/
│   │   │   ├── projects/
│   │   │   ├── analyses/
│   │   │   ├── chromatograms/
│   │   │   ├── spectra/
│   │   │   ├── workflows/
│   │   │   ├── runs/
│   │   │   └── results/
│   │   ├── routes/
│   │   ├── state/
│   │   ├── plotting/
│   │   └── types/
│   └── tests/
│
├── bindings/
│   └── r/
│       ├── DESCRIPTION
│       ├── NAMESPACE
│       ├── R/
│       ├── src/
│       │   ├── bindings/
│       │   └── Makevars
│       ├── tests/
│       └── vignettes/
│
├── integrations/
│   └── cf-streamfind/
│       ├── pyproject.toml
│       ├── src/
│       │   └── cf_streamfind/
│       └── tests/
│
├── docker/
│   ├── server.Dockerfile
│   ├── development.Dockerfile
│   └── compose.yaml
│
├── docs/
│   ├── architecture/
│   ├── development/
│   ├── api/
│   ├── migration/
│   └── decisions/
│
├── scripts/
│   ├── bootstrap.ps1
│   ├── bootstrap.sh
│   ├── build.ps1
│   ├── build.sh
│   ├── test.ps1
│   └── test.sh
│
└── .github/
    └── workflows/
```

---

# 5. Component Responsibilities

## 5.1 `core`

`core` is the authoritative native implementation.

It must manage:

- project creation and opening;
- project identifiers and metadata;
- DuckDB connection lifecycle;
- schema creation and validation;
- transactions;
- analysis import;
- file-reader dispatch;
- chromatogram and spectrum persistence;
- workflow definitions;
- method registration;
- workflow validation;
- workflow execution;
- algorithm execution;
- result persistence;
- structured logging events;
- cancellation checks;
- progress events;
- native error types;
- version and capability discovery.

It must not contain:

- Rcpp types;
- `SEXP`;
- Python objects;
- pybind11 code;
- FastAPI concepts;
- HTTP;
- React concepts;
- Cogniflow contracts;
- GUI state;
- language-specific exceptions;
- direct user-interface formatting.

## 5.2 `python`

The Python package provides:

- Pythonic project classes;
- workflow and method models;
- typed parameters;
- exception mapping;
- resource lifecycle management;
- context managers;
- progress callback adapters;
- NumPy and Arrow conversion;
- CLI commands where appropriate;
- stable public APIs for FastAPI and Cogniflow.

It must not become the authoritative location of:

- schema definitions already owned by the core;
- algorithm implementations;
- workflow execution semantics;
- DuckDB transaction logic;
- file parsing;
- project validation logic.

## 5.3 `server`

The server provides:

- REST endpoints;
- WebSocket or SSE run events;
- request validation;
- job submission;
- job isolation;
- process lifecycle;
- authentication hooks;
- upload/download handling;
- result pagination;
- binary data transfer;
- deployment configuration;
- OpenAPI generation.

The server must call the public Python API.

## 5.4 `frontend`

The frontend provides:

- project selection and creation;
- file import;
- analysis browsing;
- chromatogram and spectrum visualization;
- workflow creation and editing;
- method parameter forms;
- run submission;
- run progress;
- result exploration;
- report access;
- error presentation.

It must not access DuckDB directly.

## 5.5 `bindings`

`bindings/r` provides the R interface.

The initial goal is not to remove R functionality but to make R a consumer of the core.

The binding layer may provide:

- R6 or S7 wrappers;
- R data-frame conversion;
- R plotting helpers;
- compatibility with existing APIs;
- Quarto and R Markdown support;
- optional `reticulate` integration.

## 5.6 `integrations`

`integrations/cf-streamfind` provides the Cogniflow adapter.

It is responsible for:

- operation registration;
- Cogniflow contract mapping;
- context translation;
- StreamFind call orchestration;
- result translation;
- package metadata;
- integration-specific tests.

---

# 6. Core Architectural Boundaries

## 6.1 Core public API layers

Use three internal layers:

```text
Public API
    │
Application services
    │
Domain and infrastructure
```

### Public API

Stable types intended for consumers:

- `Project`
- `ProjectOptions`
- `ProjectInfo`
- `Analysis`
- `Workflow`
- `MethodDefinition`
- `Run`
- `RunStatus`
- `ResultSet`
- `ProgressEvent`
- `StreamFindError`

### Application services

Coordinate use cases:

- `ProjectService`
- `AnalysisService`
- `WorkflowService`
- `ExecutionService`
- `ResultService`
- `SchemaService`

### Domain and infrastructure

Implementation details:

- DuckDB repositories;
- file readers;
- algorithm registries;
- workflow planner;
- execution context;
- transaction manager;
- result writers;
- logging sinks.

Consumers should not access repository implementation classes directly.

## 6.2 Proposed C++ API direction

```cpp
namespace streamfind {

struct ProjectOptions {
    std::filesystem::path database_path;
    std::string project_id;
    std::string project_type;
    bool create_if_missing{false};
};

class Project {
public:
    static Project open(const ProjectOptions& options);
    static Project create(const ProjectOptions& options);

    const ProjectInfo& info() const;

    AnalysisImportResult add_analyses(
        const std::vector<std::filesystem::path>& files,
        const AnalysisImportOptions& options = {}
    );

    Workflow create_workflow(const WorkflowDefinition& definition);
    Workflow load_workflow(std::string_view workflow_id);

    Run submit(
        const Workflow& workflow,
        const RunOptions& options = {}
    );

    ResultSet results(std::string_view run_id) const;

    void close();
};

}
```

The public C++ API should use:

- `std::filesystem::path`;
- `std::string`;
- `std::vector`;
- `std::optional`;
- `std::chrono`;
- stable StreamFind value types;
- RAII for native resources.

Avoid exposing:

- raw DuckDB handles;
- Rcpp data structures;
- pybind11 types;
- external-library classes;
- raw ownership pointers.

---

# 7. DuckDB Ownership and Persistence

## 7.1 Core responsibility

The core owns all DuckDB interaction.

Language bindings must request operations through the core rather than issue SQL against StreamFind project databases.

## 7.2 Required storage modules

```text
core/src/storage/
├── connection.cpp
├── transaction.cpp
├── schema.cpp
├── migrations.cpp
├── project_repository.cpp
├── analysis_repository.cpp
├── workflow_repository.cpp
├── run_repository.cpp
└── result_repository.cpp
```

## 7.3 Database rules

- One project database may contain one or more logical project records only if explicitly supported.
- Project opening must validate schema version.
- Every schema change requires a migration.
- Migrations must be ordered, transactional, and testable.
- Store framework version and schema version separately.
- Algorithms must not issue ad hoc SQL from unrelated modules.
- Repository classes own SQL.
- Use prepared statements for dynamic parameters.
- Use transactions for multi-table operations.
- Add integrity checks for analysis, workflow, run, and result references.
- Define concurrency behaviour explicitly.
- Prevent simultaneous unsafe writers.
- Support read-only project opening where appropriate.

## 7.4 Schema migration model

```text
schema_migrations
├── version
├── applied_at
├── streamfind_version
└── checksum
```

Add:

- migration discovery;
- migration ordering;
- checksum validation;
- rollback policy;
- compatibility checks;
- backup recommendation for destructive migrations.

---

# 8. Algorithms and Data Processing

## 8.1 Algorithm contract

Algorithms should implement a common native interface.

```cpp
class Algorithm {
public:
    virtual ~Algorithm() = default;

    virtual AlgorithmMetadata metadata() const = 0;

    virtual AlgorithmResult execute(
        ExecutionContext& context,
        const ParameterSet& parameters
    ) = 0;
};
```

## 8.2 Algorithm registry

The core should provide a registry:

```cpp
registry.register_algorithm(
    "streamfind.mass_spec.detect_features",
    []() { return std::make_unique<DetectFeatures>(); }
);
```

Each algorithm must define:

- stable operation identifier;
- display name;
- version;
- input types;
- output types;
- parameter schema;
- validation rules;
- execution function;
- progress reporting;
- cancellation checkpoints.

## 8.3 Data access rules

Algorithms receive access through an `ExecutionContext`.

The context may provide:

- project metadata;
- transaction;
- repositories;
- temporary storage;
- progress reporter;
- cancellation token;
- logging interface;
- execution options.

Algorithms must not:

- create independent unmanaged DuckDB connections;
- depend on R;
- depend on Python;
- write arbitrary output tables without registration;
- silently change schemas.

---

# 9. Workflow Model

## 9.1 Workflow entities

A workflow should contain:

- workflow identifier;
- name;
- version;
- project compatibility;
- ordered steps or graph;
- operation identifier per step;
- validated parameter values;
- input/output bindings;
- metadata;
- creation and modification timestamps.

## 9.2 Execution states

```text
created
validated
queued
running
completed
failed
cancelled
```

## 9.3 Run records

Each run should persist:

- run identifier;
- workflow identifier and version;
- project identifier;
- status;
- start and finish timestamps;
- step status;
- resolved parameters;
- input references;
- result references;
- error information;
- log references;
- StreamFind version;
- algorithm versions.

## 9.4 Progress contract

```cpp
struct ProgressEvent {
    std::string run_id;
    std::string step_id;
    std::string operation_id;
    double progress;
    std::string message;
    ProgressLevel level;
    std::chrono::system_clock::time_point timestamp;
};
```

Bindings may translate this event to Python callbacks, WebSocket messages, R logs, or Cogniflow events.

---

# 10. Error Model

Create native error categories:

- `InvalidArgument`
- `ProjectNotFound`
- `ProjectAlreadyExists`
- `SchemaMismatch`
- `DatabaseError`
- `ReaderError`
- `UnsupportedFileFormat`
- `WorkflowValidationError`
- `AlgorithmError`
- `RunCancelled`
- `DependencyError`
- `InternalError`

Each error should include:

- stable error code;
- human-readable message;
- optional details;
- source operation;
- nested cause where appropriate.

Python maps these to typed exceptions.

FastAPI maps these to structured HTTP responses.

R maps these to conditions or errors.

---

# 11. Python Package Design

## 11.1 Build chain

```text
pyproject.toml
    │
scikit-build-core
    │
CMake
    │
pybind11 extension
    │
streamfind-core
```

## 11.2 `scikit-build-core` responsibility

`scikit-build-core`:

- invokes CMake during Python package builds;
- builds the pybind11 extension;
- installs native artefacts into wheels;
- integrates CMake with standard Python packaging;
- supports editable development builds;
- supports wheel creation.

It does not replace CMake.

## 11.3 Package layout

```text
python/src/streamfind/
├── __init__.py
├── _core/
│   └── native module
├── project/
│   ├── base.py
│   ├── mass_spec.py
│   └── chromatograms.py
├── workflow/
│   ├── models.py
│   ├── builder.py
│   └── run.py
├── analyses/
├── nta/
├── models/
├── services/
└── errors.py
```

## 11.4 Binding strategy

Bind only stable native APIs.

Use:

- opaque native handles where lifecycle is complex;
- RAII-backed Python classes;
- `py::class_`;
- `py::enum_`;
- typed result objects;
- exception translators;
- `py::gil_scoped_release` for long operations.

Do not bind every internal class.

## 11.5 Public Python API

```python
from streamfind import MassSpecProject

project = MassSpecProject.create(
    database_path="project.duckdb",
    project_id="demo",
)

project.add_analyses(
    ["sample_01.mzML", "sample_02.mzML"]
)

workflow = project.workflows.create(
    name="NTA workflow"
)

workflow.add_step(
    operation="streamfind.mass_spec.load_analyses",
    parameters={"selection": "all"},
)

workflow.add_step(
    operation="streamfind.nta.detect_features",
    parameters={
        "ppm": 5.0,
        "min_intensity": 1000.0,
    },
)

run = workflow.submit()
```

## 11.6 Python service layer

FastAPI and Cogniflow should consume services such as:

- `ProjectService`
- `WorkflowService`
- `RunService`
- `ResultService`

These services provide stable orchestration and validation around the native bindings.

---

# 12. FastAPI Server Design

## 12.1 Initial endpoints

### Projects

```text
POST   /api/v1/projects
GET    /api/v1/projects
GET    /api/v1/projects/{project_id}
DELETE /api/v1/projects/{project_id}
POST   /api/v1/projects/{project_id}/open
POST   /api/v1/projects/{project_id}/close
```

### Analyses

```text
POST   /api/v1/projects/{project_id}/analyses/import
GET    /api/v1/projects/{project_id}/analyses
GET    /api/v1/projects/{project_id}/analyses/{analysis_id}
DELETE /api/v1/projects/{project_id}/analyses/{analysis_id}
```

### Chromatograms and spectra

```text
GET /api/v1/projects/{project_id}/chromatograms
GET /api/v1/projects/{project_id}/chromatograms/{chromatogram_id}
GET /api/v1/projects/{project_id}/spectra
GET /api/v1/projects/{project_id}/spectra/{spectrum_id}
```

### Workflows

```text
POST   /api/v1/projects/{project_id}/workflows
GET    /api/v1/projects/{project_id}/workflows
GET    /api/v1/projects/{project_id}/workflows/{workflow_id}
PUT    /api/v1/projects/{project_id}/workflows/{workflow_id}
DELETE /api/v1/projects/{project_id}/workflows/{workflow_id}
POST   /api/v1/projects/{project_id}/workflows/{workflow_id}/validate
```

### Runs

```text
POST /api/v1/projects/{project_id}/workflows/{workflow_id}/runs
GET  /api/v1/projects/{project_id}/runs
GET  /api/v1/projects/{project_id}/runs/{run_id}
POST /api/v1/projects/{project_id}/runs/{run_id}/cancel
GET  /api/v1/projects/{project_id}/runs/{run_id}/events
```

### Results

```text
GET /api/v1/projects/{project_id}/runs/{run_id}/results
GET /api/v1/projects/{project_id}/runs/{run_id}/results/{result_id}
GET /api/v1/projects/{project_id}/runs/{run_id}/results/{result_id}/download
```

## 12.2 Job execution

Initial implementation:

```text
FastAPI process
    │
    ▼
Job manager
    │
    ▼
Process worker
    │
    ▼
streamfind Python API
    │
    ▼
C++ core
```

Use worker processes for:

- native crash isolation;
- cancellation;
- independent project handles;
- memory isolation;
- long-running workflows.

Threads may be used later only where native code is confirmed safe and releases the GIL.

## 12.3 Large-data responses

Use:

- pagination for tables;
- server-side filtering;
- server-side sorting;
- downsampled plotting endpoints;
- Arrow IPC;
- Parquet export;
- streamed downloads.

Do not return millions of values in ordinary JSON responses.

---

# 13. React Frontend Design

## 13.1 Initial pages

- Home
- Project Manager
- Project Overview
- Analyses
- Chromatograms
- Spectra
- Workflow Editor
- Runs
- Results
- Reports
- Settings

## 13.2 Frontend feature structure

```text
frontend/src/features/
├── projects/
├── analyses/
├── chromatograms/
├── spectra/
├── workflows/
├── runs/
└── results/
```

Each feature should contain:

- API hooks;
- page components;
- feature components;
- local schemas;
- tests;
- state where needed.

## 13.3 Workflow editor

The workflow editor should support:

- operation catalogue;
- parameter forms derived from schemas;
- ordered and graph workflows;
- validation errors;
- step configuration;
- input/output connections;
- workflow save/load;
- execution submission;
- run status overlay.

## 13.4 API generation

Generate TypeScript types or clients from FastAPI OpenAPI where practical.

This reduces drift between:

- Pydantic models;
- endpoint contracts;
- TypeScript types.

---

# 14. R Binding Migration

## 14.1 Initial goal

Keep the current R package operational while moving the native implementation under it.

## 14.2 Migration path

1. Build `streamfind-core` using CMake.
2. Link the existing Rcpp binding layer against the new library.
3. Move domain logic from Rcpp exports into the core.
4. Keep conversion code in Rcpp.
5. Preserve current R classes and public functions.
6. Deprecate direct R-only functionality only after replacement APIs exist.
7. Publish precompiled R binaries.

## 14.3 Rcpp layer should contain

- conversion from R vectors and data frames;
- R external-pointer lifecycle;
- R exception mapping;
- thin exported functions;
- R-specific callbacks;
- compatibility wrappers.

It should not contain:

- SQL construction;
- file-reader dispatch;
- workflow validation;
- algorithm implementation;
- project-state transitions.

---

# 15. Cogniflow Integration

## 15.1 Target relationship

```text
cf-streamfind
    │
    ▼
streamfind Python API
    │
    ▼
streamfind-core
```

## 15.2 Integration responsibilities

- receive Cogniflow execution context;
- resolve StreamFind project;
- map operation IRIs to Python service calls;
- map Cogniflow values to StreamFind models;
- execute;
- map StreamFind results to Cogniflow outputs;
- emit progress;
- propagate structured errors.

## 15.3 Non-goals

`cf-streamfind` must not:

- call FastAPI for local in-process execution;
- access DuckDB directly;
- import `_core`;
- define duplicate algorithms;
- duplicate workflow validation.

---

# 16. Build and Packaging

## 16.1 Root CMake

The root CMake project should support:

```text
STREAMFIND_BUILD_CORE
STREAMFIND_BUILD_TESTS
STREAMFIND_BUILD_PYTHON
STREAMFIND_BUILD_R
STREAMFIND_BUILD_SHARED
STREAMFIND_BUILD_TOOLS
```

## 16.2 CMake targets

```text
streamfind_core
streamfind_core_static
streamfind_core_tests
streamfind_python
```

Use namespaced targets:

```cmake
StreamFind::Core
```

## 16.3 Python wheels

Use:

- `scikit-build-core`;
- `pybind11`;
- `cibuildwheel`;
- repair tools appropriate to each platform.

Target wheel platforms initially:

- Windows x86_64;
- Linux x86_64.

Add macOS only after dependencies are confirmed.

## 16.4 Runtime dependencies

Define a dependency policy for:

- DuckDB;
- OpenBabel;
- HDF5;
- zlib;
- XML parsers;
- vendor readers;
- OpenMP.

For each dependency define:

- source or binary acquisition;
- build mode;
- static or dynamic linking;
- licence;
- runtime location;
- version;
- update process;
- packaging rules.

---

# 17. Testing Strategy

## 17.1 Native tests

Test:

- project creation;
- project opening;
- schema initialization;
- migrations;
- transactions;
- import failure rollback;
- file readers;
- algorithm parameter validation;
- algorithm outputs;
- workflow validation;
- workflow execution;
- run cancellation;
- result persistence;
- error mapping.

## 17.2 Python tests

Test:

- object lifecycle;
- exception mapping;
- parameter conversion;
- NumPy conversion;
- Arrow conversion;
- progress callbacks;
- GIL release behaviour;
- wheel installation;
- public API stability.

## 17.3 Server tests

Test:

- API validation;
- project endpoints;
- import endpoints;
- run submission;
- progress events;
- cancellation;
- worker failure;
- large result download;
- structured error responses.

## 17.4 Frontend tests

Test:

- project creation flow;
- import flow;
- workflow editing;
- validation rendering;
- run submission;
- progress updates;
- cancellation;
- result visualization;
- API failure states.

## 17.5 Integration tests

Create end-to-end tests:

```text
test fixture
    │
    ▼
FastAPI
    │
    ▼
Python
    │
    ▼
C++
    │
    ▼
DuckDB
    │
    ▼
validated result
```

---

# 18. Continuous Integration

## 18.1 Required workflows

- C++ build and tests;
- Python source build;
- Python wheel build;
- Python tests;
- FastAPI tests;
- frontend lint and tests;
- R binding build;
- R package tests;
- integration tests;
- dependency licence checks;
- release artefact creation.

## 18.2 CI matrix

Initial matrix:

- Windows latest;
- Ubuntu latest;
- Python 3.11;
- Python 3.12;
- Python 3.13 when dependencies support it;
- R current supported version.

## 18.3 Release outputs

- source archive;
- Python wheels;
- Python source distribution;
- server container image;
- frontend production bundle;
- R package binaries where supported;
- generated API documentation.

---

# 19. Documentation

Create architecture decision records for:

- CMake as the native build system;
- pybind11;
- scikit-build-core;
- FastAPI;
- React;
- DuckDB ownership;
- process-based worker isolation;
- binary result transport;
- R compatibility strategy;
- Cogniflow integration strategy.

Required documentation:

- build from source;
- install Python wheel;
- run FastAPI server;
- frontend development;
- add an algorithm;
- add a file reader;
- add a schema migration;
- add a workflow operation;
- create a Cogniflow adapter;
- extend R bindings;
- release process.

---

# 20. Migration Phases

## Phase 0 — Baseline and inventory

### Objective

Create an authoritative inventory of the current implementation before moving code.

### Tasks

- Catalogue current C++ files.
- Catalogue current Rcpp exports.
- Identify domain logic inside Rcpp.
- Catalogue R classes and their native dependencies.
- Catalogue DuckDB tables and schemas.
- Catalogue file readers.
- Catalogue processing methods.
- Catalogue external dependencies.
- Catalogue current Shiny workflows.
- Add baseline tests for critical project operations.
- Record current outputs for representative datasets.
- Define supported platforms.
- Define minimum compiler versions.
- Define initial Python versions.

### Deliverables

- `docs/migration/current-architecture.md`
- `docs/migration/native-inventory.md`
- `docs/migration/rcpp-logic-inventory.md`
- `docs/migration/database-schema.md`
- baseline test fixtures
- compatibility matrix

### Exit criteria

- Every current core subsystem has an owner and target location.
- Critical current behaviours have regression tests.
- No source movement begins before the inventory is complete.

---

## Phase 1 — Establish the monorepo structure

### Objective

Create the new top-level structure without changing behaviour.

### Tasks

- Add root `CMakeLists.txt`.
- Add `CMakePresets.json`.
- Add `core`, `python`, `server`, `frontend`, `bindings`, and `integrations`.
- Move the existing R package into `bindings/r` or provide a transitional layout.
- Keep build scripts operational.
- Add documentation folders.
- Add CI placeholders.
- Add common formatting and linting rules.

### Deliverables

- new repository skeleton;
- root build documentation;
- transitional R build;
- CI bootstrap.

### Exit criteria

- Existing R package can still be built.
- Empty or minimal new components can be discovered and built.
- Repository naming and ownership are documented.

---

## Phase 2 — Create standalone `streamfind-core`

### Objective

Build the existing native code independently from R.

### Tasks

- Move or mirror current `src/core` into `core/src`.
- Create public and private header boundaries.
- Add export macros.
- Create `StreamFind::Core`.
- Remove includes of Rcpp from core code.
- Replace R-specific types.
- Replace R logging calls.
- Replace R interrupt checks with cancellation tokens.
- Replace R data-frame construction with native result structures.
- Add native error types.
- Add native tests.
- Add CMake dependency targets.
- Add install rules.

### Functionality to migrate from Rcpp

Move into the core when found:

- SQL operations;
- table creation;
- project initialization;
- project validation;
- file dispatch;
- workflow construction;
- parameter validation;
- algorithm orchestration;
- result writing;
- project metadata extraction;
- run-state transitions.

Keep in Rcpp:

- `SEXP` conversion;
- R data frames;
- R external pointers;
- R exceptions;
- R callback conversion.

### Exit criteria

- Core builds without R.
- Core tests run without R.
- A native C++ test can create a project, import data, execute at least one workflow, and read results.
- Current R bindings can link to the new library.

---

## Phase 3 — Formalize storage, workflow, and algorithm APIs

### Objective

Stabilize core contracts before building language bindings.

### Tasks

- Introduce repository classes for DuckDB access.
- Introduce migration management.
- Introduce algorithm registry.
- Introduce operation identifiers.
- Introduce parameter schemas.
- Introduce workflow validation.
- Introduce run persistence.
- Introduce progress and cancellation.
- Introduce structured results.
- Add versioned public API documentation.
- Add compatibility tests.

### Exit criteria

- Algorithms are invoked through the registry.
- Workflows use stable operation identifiers.
- SQL is isolated in storage classes.
- Run state is persisted.
- Progress and cancellation work in native tests.

---

## Phase 4 — Build the Python package

### Objective

Create the public Python SDK.

### Tasks

- Add `pyproject.toml`.
- Configure `scikit-build-core`.
- Add pybind11.
- Bind stable core APIs.
- Add typed Python wrappers.
- Add Python exceptions.
- Add context managers.
- Add NumPy and Arrow conversion.
- Release GIL for long-running native calls.
- Add progress callback support.
- Add unit and integration tests.
- Build local wheels.
- Add editable development installation.

### Exit criteria

The following works from an installed wheel:

```python
from streamfind import MassSpecProject

project = MassSpecProject.create(...)
project.add_analyses(...)
workflow = project.workflows.create(...)
run = workflow.submit()
results = run.results()
```

- No R installation is needed.
- No compiler is needed for binary-wheel installation.
- `cf-streamfind` can begin consuming the API.

---

## Phase 5 — Refactor `cf-streamfind`

### Objective

Make Cogniflow consume the new public Python API.

### Tasks

- Replace duplicated native logic.
- Replace direct local implementation calls.
- Map Cogniflow contracts to Python services.
- Add operation mapping.
- Add progress mapping.
- Add structured error mapping.
- Add integration tests.
- Add distribution and repo-mode tests.

### Exit criteria

- Cogniflow executes StreamFind processing through the Python package.
- No FastAPI server is required for in-process execution.
- `cf-streamfind` does not import `_core`.
- No direct DuckDB access remains in the integration.

---

## Phase 6 — Build the FastAPI server

### Objective

Expose StreamFind as an application service.

### Tasks

- Create server project.
- Add configuration model.
- Add project registry.
- Add REST routes.
- Add job manager.
- Add process workers.
- Add run events.
- Add cancellation.
- Add binary result downloads.
- Add OpenAPI generation.
- Add server tests.
- Add Docker image.

### Exit criteria

- Projects can be created and opened through HTTP.
- Analyses can be imported.
- Workflows can be created and validated.
- Runs execute outside request handlers.
- Progress is visible.
- Results can be retrieved.
- Large result data is not forced into unbounded JSON.

---

## Phase 7 — Build the React frontend

### Objective

Create a modern GUI using the FastAPI contract.

### Tasks

- Bootstrap Vite + React + TypeScript.
- Add routing.
- Generate API types.
- Implement project manager.
- Implement analysis import.
- Implement chromatogram browser.
- Implement spectrum browser.
- Implement workflow editor.
- Implement run monitoring.
- Implement result exploration.
- Add error boundaries.
- Add tests.
- Add production build.

### Exit criteria

- Core end-user workflows can be completed without Shiny.
- Run status and failures are visible.
- Workflow validation errors are actionable.
- Large tables and plots remain responsive.

---

## Phase 8 — Rebase the R package onto the core

### Objective

Complete migration of the existing R package away from owning the native implementation.

### Tasks

- Replace old native build rules.
- Link Rcpp bindings to `streamfind-core`.
- Remove duplicate native sources.
- Move remaining domain logic out of Rcpp.
- Preserve public R APIs.
- Add deprecations where necessary.
- Publish binaries.
- Evaluate selective `reticulate` use.
- Keep Quarto and scientific reporting support.

### Exit criteria

- The R package is a binding and scientific interface.
- The R package no longer acts as the native build authority.
- RTools is not required for users installing supported binaries.
- Existing critical R workflows pass regression tests.

---

## Phase 9 — Replace Shiny and simplify deployment

### Objective

Remove the production dependency on Shiny after frontend parity.

### Tasks

- Freeze Shiny feature development.
- Compare React and Shiny feature matrices.
- Migrate remaining screens.
- Add data export and report links.
- Update documentation.
- Replace production Docker image.
- Keep a temporary compatibility release if needed.
- Remove Shiny dependencies after formal deprecation.

### Exit criteria

- React frontend covers supported GUI workflows.
- FastAPI is the GUI backend.
- Production deployment no longer requires R.
- Shiny is removed or clearly retained only as legacy compatibility.

---

## Phase 10 — Release hardening

### Objective

Prepare the new architecture for stable releases.

### Tasks

- ABI review.
- API stability review.
- schema migration testing;
- wheel repair and validation;
- runtime dependency auditing;
- licence auditing;
- memory and performance profiling;
- concurrency testing;
- failure recovery testing;
- security review;
- documentation review;
- release automation.

### Exit criteria

- Reproducible release artefacts exist.
- Installations work on clean supported systems.
- Database migrations are tested.
- Native dependency loading is reliable.
- Versioning policies are documented.

---

# 21. Recommended Implementation Order Within `core`

Migrate in this sequence:

1. errors and logging;
2. DuckDB connection and transaction management;
3. project metadata and schema;
4. project create/open/close;
5. analysis metadata;
6. file-reader interfaces;
7. chromatogram and spectrum persistence;
8. workflow definitions;
9. algorithm registry;
10. one complete processing workflow;
11. run persistence;
12. progress and cancellation;
13. remaining algorithms;
14. result query APIs;
15. migrations;
16. public API stabilization.

This sequence minimizes the risk of binding unstable low-level behaviour.

---

# 22. First Vertical Slice

Before migrating every feature, implement one complete vertical slice.

## Suggested slice

```text
Create MassSpec project
    │
Import one mzML analysis
    │
Persist analysis metadata and chromatograms
    │
Create workflow
    │
Run one processing method
    │
Persist result
    │
Read result through Python
    │
Run through FastAPI
    │
Display in React
```

## Purpose

This validates:

- CMake;
- C++ core;
- DuckDB;
- pybind11;
- scikit-build-core;
- Python package;
- FastAPI;
- React;
- packaging boundaries.

Do not migrate the entire NTA system before this slice works.

---

# 23. Definition of Done for Each Migrated Feature

A feature is complete only when:

- core implementation exists;
- core tests exist;
- no R-specific type remains in core;
- Python API exists if user-facing;
- Python tests exist;
- FastAPI endpoint exists if GUI-facing;
- OpenAPI schema is updated;
- React support exists if required;
- R compatibility is preserved or explicitly deprecated;
- Cogniflow mapping is updated if applicable;
- documentation is updated;
- no duplicate implementation remains;
- performance is at least equivalent to baseline or deviation is documented.

---

# 24. Risks and Mitigations

## Risk: hidden domain logic in R

Mitigation:

- complete Phase 0 inventory;
- add regression tests;
- migrate by use case;
- compare database outputs.

## Risk: unstable C++ API

Mitigation:

- keep internal implementation private;
- expose coarse-grained services;
- do not bind internal repository classes;
- version public contracts.

## Risk: native dependency packaging

Mitigation:

- define explicit dependency policy;
- use CI wheel validation;
- test on clean machines;
- audit DLL and shared-library loading.

## Risk: multi-process DuckDB access

Mitigation:

- document writer ownership;
- serialize project writes;
- use process-safe job coordination;
- add lock and failure tests.

## Risk: large analytical data through REST

Mitigation:

- binary formats;
- pagination;
- server-side downsampling;
- download endpoints.

## Risk: simultaneous R, Python, and server maintenance

Mitigation:

- one core implementation;
- shared operation identifiers;
- shared schemas;
- generated clients;
- staged deprecation.

## Risk: long migration period

Mitigation:

- vertical slices;
- keep current releases working;
- phase-gated acceptance criteria;
- avoid broad rewrites.

---

# 25. Initial Agent Tasks

## Task 1 — Repository inventory

Produce:

- current folder map;
- C++ source map;
- Rcpp export map;
- R-only domain logic map;
- DuckDB schema map;
- dependency map.

## Task 2 — Root CMake bootstrap

Implement:

- root CMake;
- core target;
- options;
- presets;
- native test target;
- install layout.

Do not move algorithms yet.

## Task 3 — Native project API

Implement:

- native errors;
- project options;
- create/open/close;
- DuckDB connection;
- schema validation;
- tests.

## Task 4 — Rcpp decoupling

Identify and migrate:

- project initialization;
- SQL;
- metadata;
- validation.

Keep conversion code in Rcpp.

## Task 5 — Python prototype

Implement:

- scikit-build-core;
- pybind11 module;
- `Project.create`;
- `Project.open`;
- metadata access;
- wheel test.

## Task 6 — First vertical slice

Implement end-to-end import and one processing method.

---

# 26. Final Target State

The final system should operate as follows:

```text
streamfind-core
    owns:
        projects
        DuckDB
        schemas
        readers
        workflows
        algorithms
        runs
        results

streamfind Python
    owns:
        Python API
        object ergonomics
        type conversion
        Python services

streamfind-server
    owns:
        HTTP
        WebSocket/SSE
        jobs
        uploads
        downloads
        deployment configuration

streamfind frontend
    owns:
        user interface
        plotting
        workflow editing
        run monitoring

bindings/r
    owns:
        R API
        R conversion
        compatibility
        reporting integration

integrations/cf-streamfind
    owns:
        Cogniflow mapping
        contracts
        integration behaviour
```

The key constraint throughout implementation is:

> Every endpoint must depend on the same StreamFind core behaviour. No interface may become an independent implementation of project management, storage, workflow execution, or analytical processing.
