[package]
name = "streamfind-rust-mass-spec"
version = "0.1.0"
edition = "2021"

[dependencies]
streamfind-rust-core = { path = "../core" }
serde_json = "1"
base64 = "0.22"
cfb = "0.14"
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
quick-xml = "0.38"
regex = "1"
