% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_get_suspects_screening_csv}
\alias{rcpp_get_suspects_screening_csv}
\title{Create a suspect screening table with the native Open Babel backend}
\usage{
rcpp_get_suspects_screening_csv(suspects, file = NULL)
}
\arguments{
\item{suspects}{A \code{data.frame} with required column \code{name} and at least one
structure column from \code{SMILES} or \code{InChI}. Optional columns \code{rt},
\code{ms2_positive}, and \code{ms2_negative} are preserved.}

\item{file}{Optional output CSV path. When provided, the normalized table is
written to disk.}
}
\value{
A \code{data.frame} with columns \code{name}, \code{formula}, \code{mass}, \code{rt},
\code{SMILES}, \code{InChI}, \code{InChIKey}, \code{xLogP}, \code{ms2_positive}, and
\code{ms2_negative}.
}
\description{
Normalizes suspect structures with the embedded Open Babel runtime and
returns the standard suspect-screening columns. When \code{file} is provided, the
resulting table is also written as CSV using \code{data.table::fwrite()}.
}
