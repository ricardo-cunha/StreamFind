% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_MethodsNonTargetAnalysis.R
\name{Method_NonTargetAnalysis_AnnotateComponents}
\alias{Method_NonTargetAnalysis_AnnotateComponents}
\title{Method_NonTargetAnalysis_AnnotateComponents}
\usage{
Method_NonTargetAnalysis_AnnotateComponents(
  maxIsotopes = 5L,
  maxCharge = 1L,
  maxGaps = 1L,
  ppm = 10,
  isotopeElements = c("C:1-60", "N:0-10", "O:0-20", "S:0-4", "Cl:0-6", "Br:0-4"),
  debugComponent = "",
  debugAnalysis = ""
)
}
\arguments{
\item{maxIsotopes}{Integer(1) maximum number of isotopes to consider.}

\item{maxCharge}{Integer(1) maximum charge state to consider.}

\item{maxGaps}{Integer(1) maximum number of gaps allowed in isotope
patterns. For example, \code{maxGaps = 0} only allows consecutive isotope steps,
whereas larger values allow skipped intermediate steps before the chain is
rejected.}

\item{ppm}{Numeric(1) maximum m/z tolerance in ppm used for the final
isotope, adduct, and loss assignment checks.}

\item{isotopeElements}{Character vector describing which elements are
considered during isotope annotation and, optionally, their plausible atom
count range. Each entry must be either \code{"Element"} or
\code{"Element:min-max"}, for example \code{"Cl"} or \code{"C:1-60"}. The
defaults are tuned for common environmental organic compounds such as
pharmaceuticals, pesticides, PFAS, and personal care products:
\code{c("C:1-60", "N:0-10", "O:0-20", "S:0-4", "Cl:0-6", "Br:0-4")}.
Supported element symbols are \code{"C"}, \code{"H"}, \code{"B"}, \code{"N"},
\code{"O"}, \code{"Mg"}, \code{"Si"}, \code{"S"}, \code{"Cl"}, \code{"Br"},
\code{"K"}, \code{"Ca"}, \code{"Fe"}, \code{"Cu"}, \code{"Zn"}, and
\code{"Se"}. Use explicit ranges when the expected chemistry is narrower or
broader than the defaults.}

\item{debugComponent}{Character(1) component identifier to debug.}

\item{debugAnalysis}{Character(1) analysis name to debug.}
}
\value{
A \code{Method} object of class \code{Method_NonTargetAnalysis_AnnotateComponents}.
}
\description{
Create a \code{Method} child object for annotating ion-relationship
patterns inside previously created components. The native algorithm checks
expected isotope spacing, charge states, and mass differences consistent
with adducts and in-source fragments, helping translate raw NTA peak lists
into chemically meaningful ion annotations. Isotope annotation is resolved
before adducts and neutral losses. Final isotope assignments must satisfy
the configured \code{ppm} tolerance, respect the allowed \code{maxGaps} in the
isotope chain, and match the expected relative-intensity window estimated
from the isotope composition. Simpler isotope explanations are ranked ahead
of multi-isotope combinations, which are penalized during scoring.
}
