# NTA FIDELITY BRIEF — reproduce R method implementation exactly in core/ (C++ + semantic)

Repo root: C:/Users/cunha/Documents/GitHub/streamfind (branch dev_refactoring). Windows; bash terminal; native tools need C:/ forward-slash paths. Read AGENTS.md first (no anonymous namespaces — use `streamfind::<area>_detail` or file-local `static`; don't touch core/vendor/). C++20.

## Goal
The C++ NTA workflow Methods must reproduce the R package implementation EXACTLY — same parameters, same default values, same semantics. This brief lists (A) the CURRENT default-value mismatches to fix, (B) the missing full filter_features parameter surface to add, and (C) two methods to wire that are currently absent (FilterInternalStandards, FilterSuspects). After implementing, add a C++ conformance test on the wastewater fixture that mirrors the R dev workflow at `bindings/r/dev/dev_duckdb/dev_duckdb_ms_nta.R`.

AUTHORITATIVE R SOURCE for every method's exact param names + defaults: `bindings/r/R/class_MethodsNonTargetAnalysis.R` (read it; DO NOT modify it). The R6 classes, their `validate_object.*`, and `run.*` define the exact contract.

## Current C++ executors live in
- `core/domains/mass_spec/src/processing_methods_nta.cpp` (executors read snake_case params with `.value(name, default)`). Wire params currently match `semantic/generated/catalogue.json` for each mass_spec method.
- `core/domains/mass_spec/src/register.cpp` `register_methods()` maps id→executor.
- Semantic source: `semantic/ontology/domains/mass_spec/methods.ttl` + `parameters.ttl`; regenerate with `.venv/Scripts/python.exe semantic/generate_projection.py` after changes and run `semantic/validate_semantic.py`.

## (A) Default-value mismatches — align C++ executor defaults and R6 defaults EXACTLY

Method / R default / C++ current → FIX TO (C++ should be the R default):
1. `mass_spec.find_features`: R ppmThreshold=15, noiseThreshold=250, minSNR=3, minTraces=3L, baselineWindow=200, maxWidth=100, baseQuantile=0.1. C++ currently ppm_threshold=15 ok, noise_threshold=15→**250**, min_snr=3 ok, min_traces=3 ok, baseline_window=30→**200**, max_width=30→**100**, base_quantile=0.1 ok. Also R supports EMPTY rtWindows = full range: C++ `find_features` currently requires rt_windows_min/max arrays; if both empty arrays are supplied, treat as full-RT-range (pass empty mins/maxs to find_features_impl, matching R's empty-rtwins behaviour). Verified by the impl.
2. `mass_spec.load_features_ms1`: R rtWindow=c(-2,2), mzWindow=c(-1,6), minTracesIntensity=250, mzClust=0.005, presence=0.8. C++ currently rt_window=[0,0]→**[-2,2]**, mz_window=[0,0]→**[-1,6]**, min_traces_intensity=0→**250**, mz_clust=0.003→**0.005**, presence=0.8 ok.
3. `mass_spec.load_features_ms2`: R minTracesIntensity=10, isolationWindow=1.3, mzClust=0.005, presence=0.8. C++ currently min_traces_intensity=0→**10**, isolation_window=1.3 ok, mz_clust=0.003→**0.005**, presence=0.8 ok.
4. `mass_spec.create_components`: R rtWindow=c(0,0), minCorrelation=0.8. C++ currently uses rt_window (array) + min_correlation=0.8 ok, but default rt_window should be [0,0] not [60]. Keep the len-2 semantics (offsets), default [0,0].
5. `mass_spec.annotate_components`: R maxIsotopes=5L, maxCharge=1L, maxGaps=1L, ppm=10, isotopeElements=c("C:1-60","N:0-10","O:0-20","S:0-4","Cl:0-6","Br:0-4"). C++ currently max_isotopes=10→**5**, max_charge=2→**1**, max_gaps=1 ok, ppm=5→**10**, isotope_elements default → **["C:1-60","N:0-10","O:0-20","S:0-4","Cl:0-6","Br:0-4"]**.
6. `mass_spec.group_features`: R method="internal_standards", rtDeviation=5, ppm=10, minSamples=1L, binSize=5. C++ currently method (require non-empty)→default **"internal_standards"**, rt_deviation=10→**5**, ppm=5→**10**, min_samples=2→**1**, bin_size=5 ok. method must accept "internal_standards" and "obi_warp" (validated choice).
7. `mass_spec.fill_features`: R withinReplicate=FALSE, filtered=FALSE, rtExpand=10, mzExpand=0.01, maxPeakWidth=30, minTracesIntensity=1000, minNumberTraces=5L, minIntensity=5000, rtApexDeviation=5, minSignalToNoiseRatio=3, minGaussianFit=0.2. C++ currently rt_expand=1→**10**, mz_expand=0.01 ok, max_peak_width=30 ok, min_traces_intensity=1000 ok, min_number_traces=3→**5**, min_intensity_ms1=1000→**5000** (R param is `min_intensity`), rt_apex_deviation=1→**5**, min_signal_to_noise_ratio=3 ok, min_gaussian_fit=0.5→**0.2**.
8. `mass_spec.subtract_blank`: R BlankSubtraction blankThreshold=5, rtExpand=10, mzExpand=0.005. C++ currently blank_threshold=2→**5**, rt_expand=1→**10**, mz_expand=0.01→**0.005**, min_traces_intensity (R method has NO such param — remove it from the method/semantic or keep internal default 0; do not expose it in semantics).
9. `mass_spec.correct_matrix_suppression`: R refBlankReplicate=NA_character_ (nullable), mpRtWindow=10. C++ currently mp_rt_window=60→**10**; ref_blank_replicate default "" (R NA) → accept null/empty/NA as "use per-analysis blank assignment".
10. `mass_spec.suspect_screening` + `mass_spec.find_internal_standards`: R ppm=5, sec=10, ppmMS2=10, mzrMS2=0.008, minCosineSimilarity=0.7, minSharedFragments=3L, filtered=TRUE, analyses=character()(all). C++ currently min_shared_fragments=2→**3**, filtered=false→**TRUE** (i.e. default include filtered features). ppm/sec/ppm_ms2/mzr_ms2/min_cosine_similarity defaults ok. `analysis_names` maps to R `analyses`.
11. `mass_spec.filter_features_ms2`: R top=0L, minIntensity=NA_real_, relMinIntensity=NA_real_, blankClean=FALSE, mzClust=0.005, blankPresenceThreshold=0.8, globalPresenceThreshold=0.1. C++ currently top=0, min_intensity_ms2=NaN, rel_min_intensity=NaN, blank_clean=false, mz_clust=0.005, blank_presence_threshold=0.8, global_presence_threshold=0.1 — already faithful; keep.

## (B) Extend `mass_spec.filter_features` to the FULL R parameter surface
R `Method_NonTargetAnalysis_FilterFeatures` has 30 params (all optional; NA/NA_integer_/NA disable). Add EVERY one to the semantic method + C++ executor, mapping to the existing `nta::filter_features::filter_features_impl(...)` signature (which already takes all of them — look at `nta_filters.hpp`). Wire names (snake_case) and behaviour:
- min_sn (→minSN), min_intensity (→minIntensity), min_area (→minArea), min_width (→minWidth), max_width (→maxWidth), max_ppm (→maxPPM), min_fwhm_rt (→minFwhmRT), max_fwhm_rt, min_fwhm_mz, max_fwhm_mz, min_gaussian_a (→minGaussianA), min_gaussian_mu, max_gaussian_mu, min_gaussian_sigma, max_gaussian_sigma, min_gaussian_r2, max_jaggedness, min_sharpness, min_asymmetry, max_asymmetry, max_modality (integer, nullable→hasMaxModality flag when present), min_plates, only_filled (tri-state logical: true=keep only filled, false=keep only non-filled, null/absent=disabled → maps to R onlyFilled NA), remove_filled (bool), min_size_eic (int, nullable), min_size_ms1 (int, nullable), min_size_ms2 (int, nullable), min_rel_presence_replicate (real), remove_isotopes (bool), remove_adducts (bool), remove_losses (bool).
Semantics: each numeric param is optional real (or integer for the *size/max_modality ones), null/absent = disabled. The executor must pass NA→NaN semantics to filter_features_impl exactly as the R run method passes p$minSN etc. (R passes NA to the C++ which treats it as unset). Verify against run.Method_NonTargetAnalysis_FilterFeatures in the R file. Current C++ executor passes some NaN defaults and hardcodes removeIsotopes/Adducts/Losses=false — replace with real wiring of all 30; keep `analysis_names` selection.

## (C) Wire the two missing methods (used in the R dev workflow)
1. `mass_spec.filter_internal_standards` — R Method_NonTargetAnalysis_FilterInternalStandards params: names, min_score (NA), max_error_rt (NA), max_error_mass (NA), id_levels (int array, empty=all), min_shared_fragments (int=0), min_cosine_similarity (NA). Calls `nta::filter_internal_standards::filter_internal_standards_impl(nta_data, names, minScore, maxErrorRT, maxErrorMass, idLevels, minSharedFragments, minCosineSimilarity)` (in nta_filters.hpp). Persist updated internal_standard_buffers back to DB (the R filter writes the internal-standards table; if core lacks a table, persist via the buffers persist path — inspect how the impl mutates and what table exists; at minimum keep semantics: run the impl over loaded internal_standard_buffers and persist). It must load internal_standard_buffers (from DB or from find_internal_standards output) — check how find_internal_standards populates them and reuse.
2. `mass_spec.filter_suspects` — R Method_NonTargetAnalysis_FilterSuspects: names, min_score, max_error_rt, max_error_mass, id_levels, min_shared_fragments, min_cosine_similarity (same shapes). Calls `nta::filter_suspects::filter_suspects_impl(nta_data, names, minScore, maxErrorRT, maxErrorMass, idLevels, minSharedFragments, minCosineSimilarity)`. Persist suspect_buffers.
Add both as `sf:Method` in methods.ttl (reads/writes the appropriate table; use the same result/status pattern as sibling methods), add the parameters to parameters.ttl, register them in register.cpp, declare them in processing_methods_nta.hpp.

## (D) C++ wastewater conformance test mirroring the R dev workflow
Create `core/domains/mass_spec/tests/nta_wastewater_conformance.cpp` + register in `core/domains/CMakeLists.txt` with a `STREAMFIND_WASTEWATER_DATA_ROOT` define pointing at `tests/data/mass_spec/wastewater`. Mirror `bindings/r/dev/dev_duckdb/dev_duckdb_ms_nta.R`'s ACTIVE workflow (skip GUI/plots/toxicity/run_app — those are not unit-testable). The fixture: the 18 `*_tof_ww_*.mzML` files; use `tests/data/mass_spec/wastewater/internal_standards.csv` and `suspects.csv` (same as dev uses).
Steps (mirror R + the R6 defaults; the R script's exact parameters are in the .R file):
1. Project::create mass_spec; add_analyses for all 18 file paths.
2. set_replicate_names + set_blank_names for the 6 groups (neg_blank,pos_blank,neg_influent,pos_influent,neg_o3sw_effluent,pos_o3sw_effluent ×3) — the operations are wired (mass_spec.set_replicate_names / set_blank_names).
3. find_features (R: empty rtWindows = full range, ppm 10, noise 250, minSNR 3, minTraces 3, baselineWindow 200, maxWidth 100, baseQuantile 0.99).
4. create_components (rtWindow c(-2.5,2.5), minCorrelation 0.85).
5. annotate_components (maxIsotopes 8, maxCharge 1, maxGaps 1, ppm 10, isotopeElements c("C:1-80","N:0-10","O:0-20","S:0-4","Cl:0-6","Br:0-4")).
6. load_features_ms2 (filtered FALSE, minTracesIntensity 10, isolationWindow 1.3, mzClust 0.008, presence 0.5).
7. find_internal_standards (suspects=internal_standards.csv → parse to targets w/ name, mass, rt, SMILES, InChI, InChIKey; + ms2_positive column parsed into fragments_mz_pos/fragments_intensity_pos; ppm 10, sec 15, ppmMS2 10, mzrMS2 0.008, minCosineSimilarity 0.7, minSharedFragments 3, filtered TRUE).
8. filter_internal_standards (idLevels c(1,2,3)).
9. group_features (method "internal_standards", rtDeviation 5, ppm 10, minSamples 1, binSize 5).
10. (R has FillFeatures/CorrectMatrixSuppression/SuspectScreening commented out in v3 — OPTIONAL: exercise blank_subtraction (blankThreshold 5, rtExpand 10, mzExpand 0.005) + filter_features (minIntensity 10000, removeIsotopes/Adducts/Losses TRUE) to prove new params work; keep them if they run without error, else report.)
Parse CSVs in C++: read headers, map name/formula/mass/rt/SMILES/InChI/InChIKey/xLogP and ms2_positive (or ms2_negative) "mz int; mz int; ..." strings into SuspectQuery targets (fragments_mz_pos/fragments_intensity_pos). Assert each method returns status=="finished", features exist, IS/suspect tables populated where expected (report real counts). No GUI.

## Build + verify (MUST compile/link and run)
vcvarsall x64 then `cmake -G Ninja -DSTREAMFIND_BUILD_TESTS=ON -B build/cmake/default -S .` and `cmake --build build/cmake/default -j 8 --target streamfind_mass_spec_nta_wastewater_conformance streamfind_mass_spec_nta_processing_tests streamfind_mass_spec_load_features_tests`. Run all three; no regressions. Report real output.

## Constraints
- Faithful to R: same params, same defaults, same impl calls. Do NOT redesign the algorithms.
- No anonymous namespaces; no vendor/ changes; keep existing methods working.
- Run `semantic/validate_semantic.py` and `semantic/generate_projection.py` after semantic edits.
